import React from 'react'
import Spinner from './UI/Spinner'
import './style.css'

const Loader = () => {
    return (
        <page>
            <div class='wrapper'>
                <Spinner />
            </div>
        </page>
    )
}

export default Loader